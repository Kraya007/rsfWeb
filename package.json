{
  "name": "rsf-tutorials",
  "version": "1.0.0",
  "description": "RSF Tutorials — Top-Rated Tutoring Excellence in Soweto",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.css *.xml *.txt CNAME dist/ 2>/dev/null; cp -r images dist/"
  }
}
